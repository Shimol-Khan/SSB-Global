<% provide(:page_title, 'Edit Announcement') %>
<% provide(:announcement_active, 'active') %>
<%= render 'shared/nav' %>

<div class="container-fluid">
   <div class="row justify-content-center" style="margin-top: 20vh">
      <div class="col-lg-10">
         <div class="card p-2">
            <div class="card-body">
            <h4>Update Announcement</h4>
               <%= render 'form', announcement: @announcement %>
            </div>
         </div>
      </div>
   </div>
</div>